/**
 * Represents a package-manager.
 */
export enum PackageManager {
    /**
     * Indicates the node package manager.
     */
    NPM = "npm",

    /**
     * Indicates the performant npm.
     */
    PNPM = "pnpm",

    /**
     * Indicates the yarn package manager.
     */
    Yarn = "yarn"
}
